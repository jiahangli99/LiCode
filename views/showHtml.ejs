<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script>
        function check(answer) {
            const selection = document.querySelector('input[type="radio"]:checked').value
            if(selection === answer){
                alert('yes')
            } else {
                alert('no')
            }
            console.log(typeof(selection))
            console.log(typeof(answer))
            }
    </script>
    <title>code</title>
</head>
<body>
    <ul>
            <form name ='quiz'>
                <p><%= html.name %> </p>
                <p><%= html.description %></p>
                <p><input type="radio" name="a" value="<%= html.a %>"><%= html.a %></p>
                <p><input type="radio" name="a" value="<%= html.b %>"><%= html.b %></p>
                <p><input type="radio" name="a" value="<%= html.c %>"><%= html.c %></p>
                <p><input type="radio" name="a" value="<%= html.d %>"><%= html.d %></p>
                <input type="button" value="done" onclick="check('<%= html.answer %>')">
            </form>
    </ul>
</body>
</html>